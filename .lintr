linters: linters_with_defaults(
    line_length_linter(120),
    commented_code_linter = NULL,
    object_name_linter = NULL,  # Disable to allow %||% operator
    object_usage_linter = NULL,  # Disable due to false positives with string interpolation
    object_length_linter(length = 50L),
    quotes_linter = NULL,  # Allow both single and double quotes for SQL queries
    spaces_inside_linter(),
    brace_linter(),
    commas_linter(),
    infix_spaces_linter(),
    semicolon_linter(),
    pipe_continuation_linter(),
    whitespace_linter(),
    trailing_whitespace_linter(allow_empty_lines = TRUE),
    indentation_linter(indent = 2, hanging_indent_style = "never")
    )
